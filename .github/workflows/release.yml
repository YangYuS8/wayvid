name: Legacy Release (Retired)

on:
  workflow_dispatch:

jobs:
  retired:
    name: Legacy GUI/CLI release retired
    runs-on: ubuntu-latest
    steps:
      - name: Explain retired workflow
        run: |
          echo "The legacy GUI/CLI release packaging workflow is retired."
          echo "Old wayvid desktop and CLI artifacts are no longer the active product release deliverables."
          echo "This workflow is kept only as an explicit historical marker and does not build or publish release assets."
