# Maintainer: wayvid contributors
# RETIRED REFERENCE MANIFEST
# This PKGBUILD is kept only to document the pre-LWE packaging shape.
# LWE does not currently ship an active AUR package from this manifest.
pkgname=wayvid-git
pkgver=r137.f18b503
pkgrel=1
pkgdesc="Retired reference manifest for the pre-LWE Wayvid AUR package"
arch=('x86_64' 'aarch64')
url="https://github.com/YangYuS8/wayvid"
license=('MIT')
depends=(
    'wayland'
    'mpv'
    'gcc-libs'
    'glibc'
    'zstd'
)
makedepends=(
    'rust'
    'cargo'
    'git'
    'wayland-protocols'
    'mesa'
    'libxkbcommon'
    'fontconfig'
)
optdepends=(
    'mesa: Hardware video decoding with VA-API'
    'libva-intel-driver: Intel GPU hardware acceleration'
    'libva-mesa-driver: AMD GPU hardware acceleration'
    'nvidia-utils: NVIDIA GPU hardware acceleration'
    'steam: Steam Workshop integration for Wallpaper Engine imports'
    'niri: Workspace-aware performance optimizations'
)
provides=('wayvid')
conflicts=('wayvid')
source=("git+https://github.com/YangYuS8/wayvid.git")
sha256sums=('SKIP')

pkgver() {
    cd "$srcdir/wayvid"
    printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
}

prepare() {
    cd "$srcdir/wayvid"
}

build() {
    cd "$srcdir/wayvid"

    # Legacy reference only: no active LWE build is produced here.
    :
}

check() {
    cd "$srcdir/wayvid"

    # Legacy reference only: no active LWE checks are run here.
    :
}

package() {
    cd "$srcdir/wayvid"

    # Legacy reference only: no current LWE package artifacts are installed.
    :
}
