# Legacy Crates

`crates/wayvid-gui` and `crates/wayvid-ctl` have been retired and removed from the active repository.

Their history remains available through git history and archived planning material only.

They are not part of the active product path, default workspace verification surface, or future naming/unification work.
